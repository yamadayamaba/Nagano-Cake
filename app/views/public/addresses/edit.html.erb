<h2>配送先編集画面</h2>

<%= form_with model:@address local:true do |f| %>
  <h4>宛名</h4>
  <%= f.text_field :name %>
  <h4>郵便番号</h4>
  <%= f.text_field :postal_code %>
  <h4>住所</h4>
  <%= f.text_field :address %>
  <%= f.submit '編集' %>
  <% end %>
