<h1>管理者用トップページ</h1>

<h2>注文一覧</h2>
<table>
  <thead>
    <tr>
      <th>注文日</th>
      <th>配送先</th>
      <th>支払金額(商品合計+送料)</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
    <tr>
      <td><%= order.name %> </td>
      <td><%= at_datetime %></td>
      <td><%= order.address %></td>
      <td><%= order.billing_amount %></td>
      <th><%= link_to "show", admin_order_path %></th>
    </tr>
    <% end %>
  </tbody>
</table>
